# =========================================================================
# 【通用交付模板】Python 依赖清单标准模板 (v3.0 旗舰版)
# =========================================================================

# -------------------------------------------------------------------------
# 🔒 【严禁修改/删除】：编译与构建期工具 (Cython 二进制加密必须)
# -------------------------------------------------------------------------
setuptools>=65.0.0
wheel>=0.38.0
cython>=3.0.0

# -------------------------------------------------------------------------
# 👉 【必须修改 1】：Web 核心框架与 ASGI/WSGI 容器（按需选择）
# -------------------------------------------------------------------------
# --- 【FastAPI 选型 (默认)】 ---
fastapi[standard]>=0.115.0
pydantic>2.0
pydantic-settings>=2.2.1

# --- 【Django 选型 (若为 Django 项目请注释上方，启用下方)】 ---
# django>=4.2.0,<5.0.0
# gunicorn>=21.0.0

# -------------------------------------------------------------------------
# 👉 【必须修改 2】：数据库底层驱动与 ORM 引擎（根据数据库类型二选一）
# -------------------------------------------------------------------------
# === 【方案 A：PostgreSQL 选型 (推荐，现代化轻量)】 ===
psycopg[binary]>=3.1.18
sqlmodel>=0.0.39
alembic>=1.12.1
# asyncpg>=0.29.0

# === 【方案 B：MySQL 8.0 选型 (若为 MySQL 请启用下方，注释上方方案 A)】 ===
# mysqlclient>=2.2.0        # 同步驱动 (需系统预装 libmysqlclient)
# pymysql>=1.1.0            # 纯 Python 驱动
# asyncmy>=0.2.8            # FastAPI 异步驱动
# tortoise-orm>=0.20.0

# -------------------------------------------------------------------------
# 🔑 【安全认证、密码哈希与 Ed25519 离线授权引擎】
# -------------------------------------------------------------------------
cryptography>=41.0.0        # 👈 【核心】：Ed25519 离线签名验签与 MySQL 8 RSA 认证必需！
pwdlib[argon2,bcrypt]>=0.3.0
pyjwt>=2.8.0
email-validator>=2.1.0
# passlib[bcrypt]>=1.7.4

# -------------------------------------------------------------------------
# 👉 【按需修改 4】：缓存与消息队列中间件 (可选)
# -------------------------------------------------------------------------
# redis>=5.0.0
# celery>=5.3.0

# -------------------------------------------------------------------------
# 👉 【按需修改 5】：通用 Web 工具、邮件、网络请求与重试组件
# -------------------------------------------------------------------------
emails>=1.1.2
jinja2>=3.1.4
tenacity>=8.2.3
httpx>=0.25.1
python-multipart>=0.0.9
aiofiles>=23.0.0
python-dotenv>=1.0.0

# -------------------------------------------------------------------------
# 👉 【按需修改 6】：生产监控与 APM 组件 (可选)
# -------------------------------------------------------------------------
sentry-sdk[fastapi]>=2.0.0
